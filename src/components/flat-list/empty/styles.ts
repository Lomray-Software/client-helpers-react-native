import { hp } from '@lomray/react-native-layout-helper';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  emptyBlock: {
    flex: 1,
    minHeight: hp(49.261), // 400
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default styles;
